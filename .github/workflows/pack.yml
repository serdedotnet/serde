name: Pack
on:
  push:
    branches: [ main ]
jobs:
  pack:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 10.0.0-rc.2.25502.107
    - name: Pack
      run: ./build.sh --pack
    - name: Upload package
      uses: actions/upload-artifact@v4
      with:
        name: NuGet packages
        path: artifacts/package/release/*.nupkg
